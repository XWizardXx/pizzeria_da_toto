<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PizzeriaDaTotò" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primario -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondario -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status Bar -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <item name="android:windowBackground">@drawable/pizza_sfondo</item>
    </style>

    <style name="Theme.PizzeriaDaTotò" parent="Base.Theme.PizzeriaDaTotò" />
</resources>